/** Celerey Copilot — separate from Fidelity product branding; do not rebrand. */

export const CELEREY_COPILOT_NAME = "Celerey Copilot";

export function celereyCopilotAccessDeniedMessage(): string {
  return `Your role does not have access to ${CELEREY_COPILOT_NAME}.`;
}
